using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ScavSpawnPoint : MonoBehaviour {

	//public GameObject scavPrefab;
	//private static int counter = 0;

	//void Spawn() {
		//if (Scav.count < Scav.countLimit) {
		//	Instantiate (scavPrefab, this.transform.position, this.transform.rotation);
		//}
	//}

	void Start () {
		//ScavLogic.Spawns.Add (this.transform.position);
		//Timer.Create (Spawn, "ScavSpawner" + counter, Random.Range(5.0f, 15.0f));
		//counter++;
	}

}
